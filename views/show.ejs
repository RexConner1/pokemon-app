<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pokemon</title>
</head>
<body>
    <p>
        <%= pokemon.name %> 
        (<a display="inline" href="<%= pokemon.img %>"><%= pokemon.img %></a>)
    </p>

    <img src="<%= pokemon.img %> " alt="<%= pokemon.name %>">
</body>
</html>