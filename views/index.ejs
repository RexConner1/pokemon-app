<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pokedex</title>
</head>
<body>
    <ul class="pokemon">
        <% pokemon.forEach((mon) => { %>
            <li>
                <a href="pokemon/<%= mon.id %>"><%= mon.name %></a>

                <!-- <img src="<%= mon.img %>" alt="<%= mon.name %>"> -->

                <form action="/pokemon/<%= mon.id; %>?_method=DELETE" method="POST">
                    <input type="submit" value="DELETE"/>
                </form>

                <!-- <a href="/pokemon/<%= mon.id; %>/edit">EDIT</a> -->
                <form action="/pokemon/<%= mon.id; %>/edit">
                    <input type="submit" value="EDIT"/>
                </form>
            </li>
            <br>
        <% }) %> 
    </ul>

    <nav>
        <a href="/pokemon/new">Add a New Pokemon</a>
    </nav>
</body>
</html>